import 'package:flutter/material.dart';

class SubsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Langganan Page'),
      ),
      body: Center(
        child: Text('Ini adalah halaman langganan'),
      ),
    );
  }
}
