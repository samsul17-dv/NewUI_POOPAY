import 'package:flutter/material.dart';

class MembershipPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Membership Page'),
      ),
      body: Center(
        child: Text('Ini adalah halaman membership'),
      ),
    );
  }
}
